# gmail-attachments-cleaner

Tooling for finding large Gmail attachments and duplicates (report-first, delete later).

## Setup
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
